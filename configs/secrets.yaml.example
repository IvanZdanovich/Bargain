# Bargain Trading Framework - Secrets Configuration Template
# COPY this file to secrets.yaml and fill in your values
# NEVER commit secrets.yaml to version control!

# === API Credentials ===
# Binance API credentials (leave empty for public-only access)
binance:
  api_key: ""
  api_secret: ""

  # Testnet credentials (separate from production)
  testnet_api_key: ""
  testnet_api_secret: ""

# === Future Providers ===
# Add credentials for other providers here as needed
# example_provider:
#   api_key: ""
#   api_secret: ""

